import styles from "./Hero.module.scss";

export function Hero() {
    return (
        <section className={styles.section__hero}>
            <h1>Transformando sonhos <br /> em arte.</h1>
        </section>
    );
}
